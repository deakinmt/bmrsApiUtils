# A template config file, to specify save directories, API keys, etc.
#
# bmrs_data.py is commented out by gitignore, so it is suggested to copy
# this file as that name and insert chosen save directories, api keys etc 
# here as necessary.

# from elexon portal, 'my profile', 'scripting key'
api_key__ = 'insertHere'

# choose and output directory, if saving
import sys
saveDir = sys.path[0]